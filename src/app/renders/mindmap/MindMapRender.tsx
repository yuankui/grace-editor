import React, {FunctionComponent} from 'react';
import {RenderProps} from "../renders";
import MindMap from "./v2/MindMap";

const MindMapRender: FunctionComponent<RenderProps> = (props) => {
    return <MindMap />;
};

export default MindMapRender;
