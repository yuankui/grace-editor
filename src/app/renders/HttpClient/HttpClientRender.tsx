import React, {FunctionComponent} from "react";
import {RenderProps} from "../renders";


const HttpClientRender: FunctionComponent<RenderProps> = (props) => {
    return <h1>Client Yes!</h1>
};

export default HttpClientRender;