import React, {ReactNode} from "react";
import {MaterialIcon} from "../utils";

export default class ButtonActions extends React.Component {
    render(): ReactNode {
        return <MaterialIcon value='settings_applications' />
    }
}